# خارطة طريق التصميم والوظائف — Â.M.Ŝ GROUP

ملف لتتبّع تحسينات التصميم والـ UX. الطابع المستهدف: **أونيكس أسود + ذهبي متوهّج + خلفية كوكبية (constellation) + زجاج (glass)** — كما في معالج «إضافة لكوח חדש».

الحالة: `☐ معلّق` · `🔧 قيد العمل` · `✅ تم`

> **تحديث:** كل الصفحات المرشّحة الأربع تحوّلت لمعالجات (wizards): `dealers/new` ✅ · `products/edit` ✅ · `quotes` ✅ · `purchase-orders` ✅ · `checkout` ✅. بقيت لمسات اختيارية فقط (استخراج مكوّنات مشتركة، شريط تقدّم للموبايل).

---

## الجزء الأول — معالج إضافة عميل `/admin/dealers/new` (المرجع الذهبي 🏆)

هذا المعالج هو **النموذج المعياري** الذي نريد تعميم أسلوبه على باقي التطبيق. التحسينات المطلوبة عليه لرفعه للمستوى «الخرافي»:

### الهوية البصرية
- ✅ **شاشة نجاح ذهبية بدل الخضراء** — رأس بتدرّج `gold-gradient`، علامة ✓ (SVG) داخل حلقة متوهّجة (`shadow-gold` + `ring`) مع أنيميشن `pop-in`. *(الكونفيتي مؤجّل — اختياري)*
- ✅ **كروت النوع (סוחר/קבלן) كـ Glass-cards** — خلفية زجاجية داكنة + حدّ ذهبي متوهّج عند الاختيار + أيقونة SVG مخصّصة (متجر/خوذة) بدل الإيموجي + علامة ✓ ذهبية بالزاوية + طفو عند hover.
- ✅ **Stepper متوهّج** — الخطوة الحالية بحلقة ذهبية نابضة (`animate-gold-pulse`)، والخط الواصل يتعبّأ بتدرّج ذهبي تدريجياً (animated fill) بدل القفزة اللونية.

### التجربة (UX)
- ✅ **ملخّص (Review) قبل الإنشاء** — بطاقة ذهبية بخطوة «תנאים» تعرض كل البيانات قبل الضغط على «יצירת חשבון».
- ✅ **مؤشر قوة كلمة المرور** (4 شرائح + تسمية)، وتحويل زر 🎲 لأيقونة refresh أنيقة (SVG).
- ✅ **معاينة حيّة لرسالة الواتساب** — فقاعة شات تعرض نص الرسالة قبل الإرسال بشاشة النجاح.
- ✅ **زر «نسخ الكل»** — «העתקת כל הפרטים» ينسخ الرابط+اليوزر+الباسوورد دفعة واحدة.
- ✅ **شريط تقدّم للموبايل** — «שלב X מתוך N» + بار ذهبي يظهر على الشاشات الصغيرة (داخل `WizardStepper` المشترك → يستفيد منه كل المعالجات).

### لمسات «خرافية»
- ✅ انتقال fade ناعم بين الخطوات (`key={step}` + `animate-fade-up`).
- ✅ كروت تطفو قليلاً عند hover (`hover:-translate-y-0.5`).
- ✅ **QR code** لرابط الدخول بشاشة النجاح (مكتبة `qrcode`، يُولّد client-side).
- ✅ haptic/اهتزاز خفيف عند النجاح على الموبايل (`navigator.vibrate`).
- ✅ **كونفيتي ذهبي** لحظة الإنشاء (`<Confetti>` — CSS، بلا مكتبة).

---

## الجزء الثاني — تعميم أسلوب الـ Wizard على باقي التطبيق

فحص شامل لكل صفحات التطبيق (33 صفحة) لتحديد أين ينفع نطبّق نفس فكرة المعالج الموجّه المتكامل. مرتّبة حسب الأولوية والأثر.

### 🥇 مرشّحون قويون (تحويل لـ Wizard موصى به)

#### 1. محرّر المنتج — `/admin/products/edit` *(الأولوية القصوى)*
- **الحالة الحالية:** فورم واحد ضخم (~486 سطر، ~22 حقل عبر 4 أقسام قابلة للطي + ماسح باركود + صفوف مواصفات ديناميكية).
- **لماذا wizard:** الحقول تنقسم طبيعياً لمجموعات منطقية.
- **الخطوات المنفّذة:** (1) פרטים (اسم/slug/فئة/SKU/باركود/وصف) → (2) תמונה (صورة + datasheet) → (3) תמחור (تكلفة/أسعار/مخزون/علم) → (4) מפרט + سيخوم.
- ✅ **تم التحويل لمعالج** بنفس نمط `dealers/new` (stepper مشترك، انتقال ناعم، ملخّص، أزرار ذهبية). الـ stepper **قابل للنقر بوضع التعديل** (كل البيانات محمّلة).

#### 2. أوامر الشراء — `/admin/purchase-orders`
- **الحالة الحالية:** قائمة + فورم قابل للطي + لوحة «تجديد ذكي» (smart replenishment) — وهي عملياً شبه خطوة wizard جاهزة.
- **الخطوات المنفّذة:** معالج 3 خطوات (ספק → פריטים → סיכום). لوحة «التجديد الذكي» تبقى فوق، وزر «מלא טופס רכש» يفتح المعالج **مباشرة على خطوة الأصناف** (المعبّأة).
- ✅ **تم التحويل لمعالج** (stepper مشترك قابل للنقر + ملخّص + أزرار ذهبية + انتقال ناعم).

#### 3. عروض الأسعار — `/admin/quotes`
- **الحالة الحالية:** قائمة + فورم داخل بطاقة قابلة للطي (اختيار عميل + أصناف ديناميكية + توصية سعر ذكية).
- **الخطوات المنفّذة:** معالج 3 خطوات (לקוח → פריטים [مع شريحة «💡 מומלץ»] → סיכום). إرسال الواتساب يبقى متاحاً من قائمة العروض.
- ✅ **تم التحويل لمعالج** موازٍ لأوامر الشراء (stepper مشترك + ملخّص + أزرار ذهبية).

### 🥈 مرشّح خفيف (اختياري — مكسب للموبايل)

#### 4. الدفع — `/checkout`
- **الحالة الحالية:** فورم بقسمين على صفحة واحدة (شحن + طريقة دفع) + ملخّص جانبي.
- **الخطوات المنفّذة:** (1) משלוח → (2) תשלום. الملخّص الجانبي يبقى ظاهراً دائماً، وزر التأكيد يتكيّف حسب الخطوة.
- ✅ **تم التحويل لمعالج خطوتين** (stepper مشترك + أزرار ذهبية + انتقال ناعم)، مع الإبقاء على الـ summary الجانبي.

### ⚪ لا حاجة لـ Wizard (تبقى كما هي — صقل بصري فقط)

| الصفحة | السبب |
|--------|-------|
| `/admin/products` (القائمة) | تعديل سريع داخل الصفوف + إجراء جماعي — قراءة لا إنشاء |
| `/admin/customer-prices` | إعداد (picker + جدول) لا مهمة متعددة الخطوات |
| `/admin/inventory` | فورم بسيط 5 حقول + ماسح |
| `/admin/suppliers` | 5 حقول على بطاقة واحدة |
| `/admin/categories` | تعديل داخل الصف، حقول قليلة |
| `/admin/orders` | إدارة/قراءة لا إنشاء |
| `/admin/settings` | إعدادات مجمّعة لا معاملة |
| `/cart` | قائمة للقراءة فقط |
| `/account/security` | بطاقتا إعدادات مستقلتان |
| `/account/profile` | فورمات إعدادات مستقلة |
| `/register` | صفحة ثابتة (التسجيل مغلق) |

---

## مكوّنات مشتركة مقترحة (لتوحيد الأسلوب)

لتجنّب تكرار الكود عند تعميم نمط الـ wizard، يُقترح استخراج مكوّنات قابلة لإعادة الاستخدام:

- ✅ `<WizardStepper>` — الـ stepper الذهبي المتوهّج المشترك (يدعم النقر على الخطوات + شريط تقدّم للموبايل). يُستخدم في الصفحات الخمس كلها.
- ✅ `<Confetti>` — كونفيتي CSS بلا مكتبة (شاشات النجاح).
- ✅ `<ReviewCard>` + `<ReviewItem>` (`ReviewSummary.tsx`) — بطاقة الملخّص الموحّدة. استبدلت التكرار في `dealers/new`/`products/edit` ووحّدت صفوف `quotes`/`purchase-orders`.
- ✅ `components/icons.tsx` — تجميع أيقونات SVG (`CheckIcon`, `RefreshIcon`, `StoreIcon`, `HelmetIcon`, `WhatsAppIcon`) في مكان واحد بدل تكرارها في الصفحات.
- ✅ `<ChoiceCard>` (`ChoiceCard.tsx`) — كرت الاختيار الزجاجي الذهبي (pick-one-of-N). مستخرَج من كروت النوع في `dealers/new` وجاهز لأي خطوة اختيار مستقبلية.
- ✅ `<SuccessHero>` (`SuccessHero.tsx`) — رأس شاشة النجاح الذهبي (✓ نابض + عنوان + سطر فرعي). مستخرَج من `dealers/new`؛ جاهز لأي شاشة نجاح قادمة.

---

> **مرجع النمط:** `src/app/admin/dealers/new/page.tsx` — كل معالج جديد يقلّد بنيته (state للخطوة، تحقق متدرّج `canNext`، stepper، شاشة نجاح).
