# دليل الشاشات والأزرار — Tiandy Store

> دليل بصري/وظيفي بالعربي لكل شاشة في التطبيق: **شو وظيفتها، مين بيشوفها، وكل زر شو بيعمل.**
> الهدف: تفهم الواجهة بسرعة بدون ما تنبش بالكود.
>
> 🧠 **قاعدة العمل ("اسأل حالك"):** قبل أي تعديل على شاشة، افتح قسمها هون، افهمها زر-زر، واسأل حالك: شو الهدف؟ مين بيشوف شو؟ وين الأزرار بتتغيّر حسب الدور؟ — وبعد التعديل حدّث هالملف.

## كيف نقرأ هالدليل

كل شاشة موثّقة بنفس القالب:
- **المسار** (الـ URL) و**مين بيوصلها**.
- **الهدف** بسطر واحد.
- **مين بيشوف شو** (زائر / تاجر معتمد / أدمن / سوبر أدمن).
- **الأزرار والعناصر** — جدول: العنصر ← شو بيعمل.

الأدوار:
- **زائر** — مش مسجّل دخول. بيشوف الكتالوج بلا أسعار.
- **تاجر/مقاول معتمد** (`dealer`, approved) — أسعار + سلة + طلب.
- **أدمن / سوبر أدمن** — لوحة الإدارة كاملة (السوبر أدمن كمان بيدير المدراء ويشوف التكلفة/الربح).

---

## شريط التنقّل (Header) — مشترك بكل الشاشات

بيظهر فوق وتحت (موبايل) لكل المسجّلين. مصدره `src/components/Header.tsx`.

### الشريط العلوي (ديسكتوب)

| العنصر | شو بيعمل | مين بيشوفه |
|--------|----------|------------|
| الشعار **Â.M.Ŝ GROUP** | يرجّع للرئيسية (`/home` للمسجّل، `/` للزائر) | الكل |
| **קטלוג** | يفتح كتالوج المنتجات `/products` | الكل |
| **ההזמנות שלי** | طلباتي `/account/orders` | المسجّل |
| **הצעות מחיר** | عروض الأسعار `/account/quotes` | المسجّل |
| **חשבון** | الحساب الشخصي `/account` (أمان/بصمة/خروج) | المسجّل |
| **ניהול** | لوحة الإدارة `/admin` | أدمن فقط |
| زر البحث **⌘K** | يفتح البحث السريع (Command Palette) | المسجّل (ديسكتوب) |
| **עגלה** + عدّاد | السلة `/cart` (الرقم = عدد الأصناف) | الكل |
| الأفاتار (دائرة) | ملفي الشخصي `/account/profile` | المسجّل |
| **יציאה** | تسجيل خروج ثم الرجوع للرئيسية | المسجّل |
| **כניסת סוחרים** | صفحة الدخول | الزائر |

### الشريط السفلي (موبايل — ٤ خانات)

| الخانة | الوجهة | ملاحظة |
|--------|--------|--------|
| **ראשי** | `/home` (مسجّل) أو `/` (زائر) | |
| **קטלוג** | `/products` | |
| **עגלה** | `/cart` | بادج بعدد الأصناف |
| الخانة الرابعة | **ניהול** (أدمن) / **חשבון** (تاجر) / **כניסה** (زائر) | تتغيّر حسب الدور |

> أعلى الشريط بالموبايل كمان: زر بحث 🔍، أفاتار، و**יציאה**/**כניסה**.

---

## شاشة الكتالوج — `/products`

**المصدر:** `src/app/products/page.tsx` · **مين بيوصلها:** الكل (زائر ومسجّل).

**الهدف:** تصفّح المنتجات، الفلترة والبحث، وفتح صفحة كل منتج. الأسعار للتجار المعتمدين فقط.

### مين بيشوف شو
- **زائر:** شبكة المنتجات + صورهم وأسمائهم، **بلا أسعار** (مكتوب "מחיר לסוחרים")، مع شريط تنبيه أعلى الصفحة "الأسعار والطلب للتجار المعتمدين فقط · כניסה". ما في فرز.
- **تاجر معتمد:** أسعار + شارة "במלאי"/"אזל" + قائمة الفرز.
- **سوبر أدمن:** بالإضافة، سطر صغير بكل بطاقة فيه **سعر البيع / التكلفة / الربح والهامش%** (مخفي عن الباقين تلقائياً من قاعدة البيانات).

### الأزرار والعناصر

| العنصر | شو بيعمل |
|--------|----------|
| حقل **"חיפוש לפי שם או מק״ט"** + زر **חפש** | يبحث بالاسم العبري **أو** رقم الصنف (SKU) |
| زر **📷 סריקה** | يفتح ماسح الباركود/QR بالكاميرا — لمّا يمسح كود: يفتح المنتج مباشرة إذا تطابق، وإلا يبحث فيه |
| زر **קטגוריות ▾** (موبايل فقط) | يفتح/يغلق درج التصنيفات |
| قائمة **מיון** (فرز) | افتراضي / حسب الاسم / السعر تصاعدي / السعر تنازلي — *تظهر للي بيشوف الأسعار فقط* |
| مربع **"זמין במלאי בלבד"** | يعرض المنتجات المتوفرة بالمخزون فقط |
| الشريط الجانبي: **כל הקטגוריות** + اسم كل تصنيف | يفلتر الشبكة حسب التصنيف المختار |
| **بطاقة منتج** (ProductCard) | الضغط عليها يفتح صفحة المنتج `/product?slug=…` |
| شارة **אזל** على البطاقة | تظهر إذا نفد المخزون (للمسجّلين) |
| عدّاد **"N מוצרים"** فوق الشبكة | عدد النتائج الظاهرة بعد الفلترة |

> 🔎 **ملاحظات سلوك:** الكتالوج يعرض المنتجات المتاحة للبيع فقط (`is_orderable = true`) — الأدمن بيخفي منتج بإلغاء "למכירה" من `/admin/products`. الباركود بيتطابق بالتدرّج: باركود ← SKU ← slug.

---

## شاشة عروض الأسعار (أدمن) — `/admin/quotes`

**المصدر:** `src/app/admin/quotes/page.tsx` · **مين بيوصلها:** أدمن / سوبر أدمن فقط (محمية بـ `AdminLayout`).

**الهدف:** إنشاء عرض سعر لزبون بمعالج 3 خطوات، إرساله بالواتساب، ومتابعة العروض وتحويلها لطلبات (مع خصم المخزون).

### معالج الإنشاء (3 خطوات: לקוח → פריטים → סיכום)
- **خطوة الزبون:** اختيار زبون (تجار/مقاولين فقط) + صلاحية + ملاحظات.
- **خطوة المنتجات:** إضافة بصرية من `ProductPickerGrid`، ولكل سطر: صورة، **شارة رصيد المخزون** (במלاي: N / "פחות מהכמות" / "אזל מהמלاي"), كمية، سعر وحدة، مؤشر هامش الربح، وتوصية سعر ذكية 💡.
- **خطوة المراجعة:** ملخّص الزبون والمجموع وعدد الأصناف.

### الأزرار والعناصر

| العنصر | شو بيعمل |
|--------|----------|
| زر **+ הצעת מחיר חדשה / ביטול** | يفتح/يغلق المعالج. الإغلاق بيمسح المسودّة المحفوظة |
| **شارة رصيد المخزون** بسطر المنتج | أخضر = متوفّر، أصفر = الرصيد أقل من الكمية المطلوبة، أحمر = نفد |
| رسالة **"כמות חייבת להיות 1 לפחות"** | تظهر بحدّ أحمر إذا الكمية < 1 (بدل تجاهل السطر بصمت) |
| زر **💡 מומלץ** | يضبط سعر الوحدة على التوصية الذكية (سعر الزبون التاريخي / كلفة+ / المחيرון) |
| حقل **חיפוש לפי מספר או לקוח** | يفلتر جدول العروض بالرقم أو اسم الزبون |
| قائمة **כל הסטטוסים** | فلترة حسب الحالة (טיוטה/נשלחה/הומרה…) + عدّاد "N מתוך M" |
| زر **טען עוד** | يحمّل دفعة إضافية (50) — العرض يبدأ بأحدث 50 |
| رابط **צפייה** | يفتح عرض السعر للعميل `/quote?id=` |
| رابط/نص **וואטסאפ** | يفتح محادثة الزبون برسالة جاهزة؛ يظهر **رمادي معطّل** مع tooltip إذا الزبون بلا هاتف |
| زر **סימון כנשלחה** | يحوّل حالة المسودّة إلى "נשלחה" |
| زر **המרה להזמנה** | يحوّل العرض لطلب ويخصم المخزون — تأكيد فيه رقم العرض/المجموع/عدد الأصناف |

> 🔎 **ملاحظات سلوك:**
> - **حماية البيع الزائد:** التحويل لطلب يمرّ عبر `convert_quote_to_order` اللي **بتفحص توفّر المخزون قبل الخصم** — لو أي صنف رصيده أقل من المطلوب بتفشل العملية برسالة "אין מספיק מלاي עבور …" (مع الرصيد المتوفّر والمطلوب) بدل ما تخلّي الرصيد سالب.
> - **حفظ تلقائي للمسودّة:** العرض غير المكتمل بينحفظ بالـ `localStorage` وبيرجع لو فتحت المعالج من جديد؛ بينمسح عند الحفظ أو الإلغاء.

---

## شاشات أخرى (للتوثيق لاحقاً)

> القالب جاهز — رح ننعبّيها تدريجياً عند العمل على كل شاشة:
>
> - صفحة الدخول `/`
> - صفحة المنتج `/product`
> - السلة `/cart` والدفع `/checkout`
> - الحساب الشخصي `/account` (+ orders, quotes, security, profile)
> - لوحة الإدارة `/admin` (Operations Center + الإجراءات السريعة)
> - الإدارة: products, dealers, dealers/new, quotes, customer-prices, inventory, suppliers, purchase-orders, admins, settings
